;
; Stabilization test
;

.section text

start:
.global start
    cachei 5 ; Politely ask the CPU to fuck off
    add t0, zero, zero
    add t1, zero, zero
    add t2, zero, zero
    add t3, zero, zero
    add t4, zero, zero
    add t5, zero, zero
    add a0, zero, zero
    add a1, zero, zero
    add a2, zero, zero
    add a3, zero, zero
    add s0, zero, zero
    add s1, zero, zero
    add s2, zero, zero
    add s3, zero, zero
    add s4, zero, zero
    add s5, zero, zero
    add s6, zero, zero
    add s7, zero, zero
    add s8, zero, zero
    add s9, zero, zero
    add s10, zero, zero
    add s11, zero, zero
    add s12, zero, zero
    add s13, zero, zero
    add s14, zero, zero
    add s15, zero, zero
    add s16, zero, zero
    add s17, zero, zero

.section data
.dl 0
.section bss
.dl 0
